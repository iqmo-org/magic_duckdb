[tool.ruff]
exclude = [
    ".git",
    "__pycache__",
    "build",
    "dist",
    "versioneer.py",
    "docs/source/conf.py"]
ignore = [
    #E203, 
    #"W503", 
    #"W293", 
    #"W291",
    "E501" ]

[tool.ruff.per-file-ignores]
    "__init__.py" = ["F401"]

[tool.pyright]
    # nothing
    exclude = ["tests"]